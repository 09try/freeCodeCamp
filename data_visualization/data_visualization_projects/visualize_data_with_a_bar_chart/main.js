document.addEventListener('DOMContentLoaded', function() {
    var svg = d3.select('#main')
        .append(svg)
        .attr('width', 200)
        .attr('height', 200);

    console.log('done');
});